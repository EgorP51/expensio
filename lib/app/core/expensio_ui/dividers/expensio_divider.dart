import 'package:flutter/material.dart';

class ExpensioDivider extends StatelessWidget {
  const ExpensioDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
      thickness: 1,
      color: Colors.black54,
    );
  }
}
